# GitHub 开发工作流程指南

## 🔄 日常开发流程

### 1. 开始开发前 - 拉取最新代码

```bash
cd /home/dministrator/decklist-android

# 查看当前状态
git status

# 拉取远程最新代码（如果有其他设备推送的更改）
git pull origin main
```

---

### 2. 进行开发工作

修改代码、添加新功能等...

**常用 Git 命令**：
```bash
# 查看修改的文件
git status

# 查看具体修改内容
git diff

# 查看未跟踪的文件
git ls-files --others --exclude-standard
```

---

### 3. 提交更改到本地仓库

```bash
# 添加所有修改的文件
git add .

# 或者添加特定文件
git add app/src/main/java/com/mtgo/decklistmanager/...

# 提交更改（写清晰的提交信息）
git commit -m "feat: 添加深色模式支持"
```

---

### 4. 推送到 GitHub

```bash
# 推送到远程仓库
git push origin main

# 或者简写（因为已设置跟踪）
git push
```

---

## 📝 提交信息规范

使用有意义的提交信息，格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 常用类型 (type)
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具链相关

### 示例
```bash
git commit -m "feat(search): 添加卡牌搜索功能

- 支持按卡牌名搜索
- 支持模糊匹配
- 显示搜索结果数量

Closes #123"
```

简化版（日常使用）：
```bash
git commit -m "添加深色模式"
git commit -m "修复卡牌点击问题"
git commit -m "更新 README 文档"
```

---

## 🌿 分支管理（可选）

### 创建新分支开发功能

```bash
# 创建并切换到新分支
git checkout -b feature/dark-mode

# 在新分支上开发...
git add .
git commit -m "feat: 添加深色模式基础样式"

# 推送分支到 GitHub
git push -u origin feature/dark-mode
```

### 合并分支到主分支

```bash
# 切换回主分支
git checkout main

# 拉取最新代码
git pull origin main

# 合并功能分支
git merge feature/dark-mode

# 推送合并结果
git push origin main

# 删除本地分支（可选）
git branch -d feature/dark-mode

# 删除远程分支（可选）
git push origin --delete feature/dark-mode
```

---

## 🔍 查看历史

```bash
# 查看提交历史
git log --oneline

# 查看最近 5 条提交
git log --oneline -5

# 查看图形化历史
git log --graph --oneline --all

# 查看某文件的修改历史
git log --oneline -- app/src/main/java/com/mtgo/decklistmanager/MainActivity.kt
```

---

## ↩️ 撤销操作

### 撤销未提交的修改

```bash
# 撤销工作区的修改（恢复到上次提交）
git checkout -- <file>
git checkout -- .  # 撤销所有修改

# 撤销暂存区的修改（保留文件内容）
git reset HEAD <file>
git reset HEAD .  # 撤销所有暂存
```

### 撤销已提交的更改

```bash
# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1

# 撤销最后一次提交（丢弃修改）
git reset --hard HEAD~1

# 撤销最近 3 次提交（保留修改）
git reset --soft HEAD~3
```

### 撤销已推送的提交（危险！）

```bash
# ⚠️ 谨慎使用！会改写历史
git reset --hard HEAD~1
git push --force

# 更安全的方式：创建反向提交
git revert HEAD
git push
```

---

## 🏷️ 标签管理

为重要版本打标签：

```bash
# 创建标签
git tag v2.4.0 -a -m "Release version 2.4.0"

# 推送标签到 GitHub
git push origin v2.4.0

# 推送所有标签
git push origin --tags

# 查看所有标签
git tag

# 删除标签
git tag -d v2.4.0
git push origin --delete v2.4.0
```

---

## 📦 克隆到其他设备

### 在新设备上开始开发

```bash
# 克隆仓库
git clone git@github.com:kaorou-bot/decklist-android.git

# 进入项目目录
cd decklist-android

# 配置 Git 用户（如果还没配置）
git config user.email "496291727@qq.com"
git config user.name "MTGO Decklist Developer"

# 开始开发...
```

---

## 🐛 常见问题

### 问题 1：推送冲突

```bash
# 尝试推送时遇到冲突
git push

# 解决方法：先拉取，处理冲突，再推送
git pull origin main
# 解决冲突...
git add .
git commit -m "解决合并冲突"
git push
```

### 问题 2：合并后出现大量文件

```bash
# 清理未跟踪的文件
git clean -fd

# 清理所有未跟踪文件（包括 .gitignore 中的）
git clean -fdx
```

### 问题 3：忘记添加 .gitignore

```bash
# 停止跟踪已提交但应忽略的文件
git rm --cached build/
git add .gitignore
git commit -m "chore: 添加 .gitignore"
```

---

## 🚀 快速命令参考

```bash
# 日常开发循环
git pull origin main        # 1. 拉取最新代码
# ... 修改代码 ...
git add .                   # 2. 添加修改
git commit -m "描述"         # 3. 提交
git push origin main        # 4. 推送

# 查看状态
git status                  # 工作区状态
git log --oneline -5        # 最近提交
git diff                    # 修改内容

# 分支操作
git branch                  # 查看分支
git checkout -b 新分支       # 创建分支
git checkout main           # 切换分支
git merge 分支名            # 合并分支
```

---

## 📋 开发检查清单

每次提交前检查：

- [ ] 代码已编译通过 (`./gradlew assembleDebug`)
- [ ] 提交信息清晰明确
- [ ] 不包含敏感信息（密钥、密码等）
- [ ] 新文件已添加到 `.gitignore`（如果需要）
- [ ] 测试过基本功能
- [ ] 更新了相关文档

---

## 🎯 推荐开发流程

1. **早晨开始开发时**：
   ```bash
   git pull origin main
   ```

2. **开发过程中**：
   - 频繁提交（小步快跑）
   - 每完成一个小功能就提交一次
   - 使用清晰的提交信息

3. **结束开发时**：
   ```bash
   git add .
   git commit -m "今天的工作总结"
   git push origin main
   ```

4. **发布新版本时**：
   ```bash
   # 更新版本号
   # 运行完整测试
   git tag v2.4.0 -a -m "Release v2.4.0"
   git push
   git push --tags
   ```

---

## 💡 提示

1. **提交前先 Pull**：避免冲突
2. **小步提交**：不要一次提交太多更改
3. **写好提交信息**：方便以后查看历史
4. **定期推送**：不要只在本地堆积提交
5. **使用 .gitignore**：避免提交不必要的文件

---

**现在你可以在 GitHub 上愉快地开发了！** 🎉
